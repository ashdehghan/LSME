# Root-level pyproject.toml for pytest configuration

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = "test_*.py"
python_functions = "test_*"
addopts = "-v"
markers = [
    "slow: marks tests as slow (training-intensive)",
    "gpu: marks tests requiring CUDA",
]
